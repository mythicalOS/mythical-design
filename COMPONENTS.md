# Component registry — mythical design system

One row per component. **This is the dispatch surface**: to update a product's UI, point a
coding agent at a row (its ID + version), its card under `ds/`, and the implementation — see "Dispatching a coding agent" below.

Spec version semantics: `v1` = the v0.5 book (`design.md`). A design change bumps the
version here and the component's card under `ds/`. An implementation is **current** when its
"implements" column matches the spec version.

**Packaged vs product-local.** A row reading *PACKAGED* ships from `@mythicalos/*` and every
product consumes the same code. A row reading *built in-product* exists in exactly one product
and is a candidate for extraction — the distinction is the point of this table, because a
second product wanting that component is the moment the duplication starts costing.

## Components

| ID | Spec | Status | Spec anchor (card in `ds/`) | Implementation | implements |
|----|------|--------|------------------------------|------------------------------|------------|
| `app-shell` | v1 | shipped | `ds/layouts-app-shell.html` | **@mythicalos/shell** — `TopBar`, `NavTabs`, `ProductSwitcher`, `WorkspaceSplit`, `Rail*`, `SettingsLayout`/`SettingsNav` | shell 0.4.0 |
| `button` | v4 | shipped | `ds/components-buttons.html` | **@mythicalos/preact-ui** `Button` · **@mythicalos/react-ui** `Button` (classes from **@mythicalos/ui-core** `buttonClass`) | preact-ui 0.4.0 — v2 secondary boundary is `--my-control-border` (decision #21). v3 (ui-core 0.6.0/preact-ui 0.7.0/react-ui 0.5.0, unreleased): tone-filled variant — `tone` prop → `.btn--tone` + `data-tone` (ok/warn/error/info; bare = accent); ink rides `--my-tone-ink` (tokens 0.5.17-pending); hover/active derived via `color-mix(… 88%/76%, --my-ink)`; `error` fill stays rule-9-scoped. Spec originated pane-side (`SPEC-btn-tone.md`) during the mockup rewire; `.btn--danf` from the pane draft was dropped — zero consumers, use `data-tone="error"`. v4 (ui-core 0.7.0/preact-ui 0.8.0/react-ui 0.6.0, unreleased): tone-OUTLINED variant `.btn--tone-line` — the outline mirror on the same `data-tone` axis (the `.btn--dan` recipe per tone: surface fill, tone border+ink, tone-soft hover); the `error` arm is equivalent to `.btn--dan` and ships anyway so the axis is uniform, `.btn--dan` stays the danger-outline contract (rule 9); `muted` is NOT a tone — it maps to plain `.btn--sec` (rule 2) |
| `input` / `secret-slot` / `toggle` / `chip-dropdown` | v3 | shipped | `ds/components-inputs.html` | **@mythicalos/preact-ui** `Input`, `Toggle`, `Checkbox`, `MaskedSecretInput` (+ the opt-in password reveal) · react-ui mirror | preact-ui 0.4.0 — v3 control-border resting boundary; chip-dropdown grew a dedicated atom — see the `chip` row |
| `gauge` (arc + bar) | v1 | shipped | `ds/components-gauges.html` | **@mythicalos/preact-ui** `Gauge` (geometry + tone from **@mythicalos/ui-core** `gaugeGeom`/`gaugeTone`) | preact-ui 0.4.0 — thresholds are the package's; a consumer must not redefine them |
| `session-card` | v1 | shipped — PACKAGED | `ds/components-session-card.html` | **@mythicalos/preact-ui** `SessionCard` · react-ui mirror (logic in **@mythicalos/ui-core**) | preact-ui 0.4.0 — absence is not zero and unknown is not idle are pinned by tests |
| `terminal` / `queue-row` / `send-bar` | v2 | shipped — PACKAGED | `ds/components-terminal.html` | **@mythicalos/preact-ui** `Terminal`, `QueuePanel`, `QueueRow`, `SendBar` · react-ui mirror | preact-ui 0.4.0 — pane is heritage-dark in BOTH themes; ASAP takes the first turn gap (never "interrupts") |
| `wizard` | v1 | built in-product, NOT packaged | `ds/components-wizard.html` | product-local: `ui/src/components/WizardFrame.tsx` + the wizard page | — six steps derived from facts, not a stored counter |
| `confirm-dialog` | v1 | shipped | `ds/components-dialogs.html` | **@mythicalos/preact-ui** `ConfirmDialog`/`Scrim` (+ `typedNameMatches` in ui-core) | preact-ui 0.4.0 |
| `toast` | v1 | shipped | `ds/components-toasts.html` | **@mythicalos/preact-ui** `ToastProvider`/`useToast` | preact-ui 0.4.0 |
| `empty-state` | v1 | shipped | `ds/components-empty-states.html` | **@mythicalos/preact-ui** `EmptyState` | preact-ui 0.4.0 |
| `save-bar` | v1 | shipped — PACKAGED | `ds/layouts-settings.html` | **@mythicalos/preact-ui** `SaveBar` · react-ui mirror | preact-ui 0.4.0 — takes human labels, renders null when clean |
| `family-tile` | v1 | built in-product, NOT packaged | `ds/components-org-family.html` | product-local: the settings Family section | — detection is real; copy is design-owned |
| `stat-tiles` | v2 | shipped — PACKAGED | `ds/components-stat-tiles.html` | **@mythicalos/preact-ui** `StatTiles` · react-ui mirror | preact-ui 0.4.0 — takes a generic tile list, not a product view-model. v2 (ui-core 0.6.0, ported upstream from the Sága mockup rewire): `ok` tone — voice-only (`--my-ok` value on the default border; only trouble gets the border band); the card gained the tone-axis section |
| `git-chip` | v1 | shipped — PACKAGED | `ds/components-git-chip.html` | **@mythicalos/preact-ui** `GitChip` · react-ui mirror | preact-ui 0.4.0 — an unavailable summary never renders as a zero-count clean row; unpushed is error-toned |
| `chip` / `chip-flag` / `chip-dropdown` | v3 | shipped — PACKAGED | `ds/components-chip.html` | **@mythicalos/preact-ui** `Chip`, `ChipFlag`, `ChipDropdown` · react-ui mirror (classes from **@mythicalos/ui-core** `chipClass`/`chipFlagClass`/`chipDropdownClass`) | **RESOLVED 2026-07-28: Chip absorbed the short-lived tags family** (maintainer redesign — the Tag/Flag atoms shipped and were folded back the same day). Chip keeps its quieter neutral default (`--my-surface-hover`), gains the full range: 8 tones, xs/md sizes, dot/count/removable ×. Pill = never interactive; the × is a focusable child; `ChipDropdown` owns no menu — pair with `Popover`. v3 (ui-core 0.7.0, unreleased): `info` flag arm — the mockups draw info-MEANING machine facts (review pass, on-done delivery, observation kind) distinct from genuinely neutral ones (boot, reference, dim); the card's earlier "no info flag" doctrine is rewritten with the arm, `neutral` stays for facts with no status meaning at all |
| `theme-toggle` | v1 | shipped — PACKAGED | `ds/components-theme-toggle.html` | **@mythicalos/preact-ui** `ThemeToggle`, `ThemeToggleIcon`, `ThemeToggleSwitch` · react-ui mirror (mode logic in **@mythicalos/ui-core** `theme-toggle`: `ThemeMode`, `resolveThemeIsDark`, class derivations) · state + wiring in **@mythicalos/shell** `useThemeMode` / `ThemeControl` (0.5.0) | — three members, one component: segmented is the default with System first-class; icon shows the theme you're in (sun on light) and skips System; switch only under an explicit "Dark mode" label. Controlled only — persistence stays with the app. Terminal panes never follow (rule 3; `--my-shadow-knob` is a documented terminal residual). **Ruled 2026-07-28:** the pill stays — rule 10 carries the scoped theme-toggle exception (tokens.css rule 10); the inputs Toggle stays squared |
| `card` | v1 | shipped — PACKAGED | — (no card yet) | **@mythicalos/preact-ui** `Card` · react-ui mirror | preact-ui 0.5.0 |
| `avatar` | v1 | shipped — PACKAGED | — (no card yet) | **@mythicalos/preact-ui** `Avatar` · react-ui mirror | preact-ui 0.5.0 |
| `status-line` | v1 | shipped — PACKAGED | — (no card yet) | **@mythicalos/preact-ui** `StatusLine` · react-ui mirror (classes from **@mythicalos/ui-core** `statusLineClass`) | preact-ui 0.5.0 |
| `search-input` | v1 | shipped — PACKAGED | — (no card yet) | **@mythicalos/preact-ui** `SearchInput` · react-ui mirror | preact-ui 0.5.0 |
| `banner` | v2 | shipped — PACKAGED | `ds/components-banner.html` | **@mythicalos/preact-ui** `Banner` · react-ui mirror (tone + required glyph from **@mythicalos/ui-core** `BANNER_ICON`) | preact-ui 0.5.0 — rule #7: the glyph always rides along. v2 (ui-core 0.6.0, ported upstream from the Sága mockup rewire): non-status SURFACE variants `accent` (emphasis — accent-soft fill, accent-strong ink) + `neutral` (quiet notice — surface-hover fill); default glyphs ◆/○, consumers usually override with product SVG via `glyph`. Card (2026-07-29): status quartet with canonical glyphs, surface variants, pulse-dot glyph (`.my-pulse` rider — 1s, transient only, reduced-motion guard), trailing action |
| `token-gate` | v1 | shipped — PACKAGED | — (no card yet) | **@mythicalos/shell** `TokenGate` — the family unlock screen | shell 0.4.0 — placeholder never states a token length/alphabet (formats differ per product) |
| `tier-bar` / `memory-card` | v1 | built in-product, NOT packaged | `ds/components-memory.html` | product-local: the Memory page exists (`ui/src/pages/memory.tsx`) — never extracted | — registry previously claimed the page did not exist |
| `project-hero` | v2 | built in-product, NOT packaged | `ds/components-project-hero.html` | product-local: `ui/src/pages/projects.tsx` detail hero | — registry previously claimed unimplemented |
| `popover` | v2 | shipped — PACKAGED | `ds/components-popover.html` | **@mythicalos/preact-ui** `Popover` · react-ui mirror (placement + roving focus in ui-core; rich-row derivation `popItemClass`) | preact-ui 0.4.0. v2 (ui-core 0.7.0, unreleased): rich rows — elements on the same `__item`, never a fork: `--rich`, avatar `__av` (selection flips it accent/`--my-surface`), the leading fixed-width `__lead` check column (ruled: rich rows LEAD — alignment survives mixed rows; the plain item keeps its trailing `__check`), `__body`/`__name`/`__sub`, `__group` section ruler (a different job than `__head`), `is-dim` (disabled-ink, still clickable — never opacity) |
| `select` | v3 | shipped — PACKAGED | `ds/components-select.html` | **@mythicalos/ui-core** `<mythical-select>` — form-associated web component, native `<select>` parity, groups, progressive mode, `variant="chip"`. Framework-agnostic: usable as-is from Preact or React. | ui-core 0.3.2 — v3 APG combobox pattern; popup is border-box and pinned to both edges (0.3.x) |
| `org-card` | v1 | built in-product, NOT packaged | `ds/components-org-family.html` | product-local: the settings rail footer | — registry previously claimed unimplemented |
| `timeline` | v1 | NEW — unimplemented | `ds/components-timeline.html` | not built — the natural consumer is a scheduler's forward/backward view | — dot and rail derive from the same two custom properties so the dot cannot drift off the line |
| `file-explorer` / `markdown-preview` | v1 | shipped — PACKAGED | `ds/components-file-explorer.html` | **@mythicalos/preact-ui** `FileTree`, `FilePreview`, `FileScopePicker` · react-ui mirror | preact-ui 0.4.0 — read-only; unavailable/empty/too-large/binary are first-class states |
| `stepper` | v1 | shipped — PACKAGED | `ds/components-stepper.html` | **@mythicalos/preact-ui** `Stepper` · react-ui mirror (classes from **@mythicalos/ui-core** `stepperClass`/`stepClass`/`stepBadge`/`stepState`) | ui-core 0.7.0/preact-ui 0.8.0/react-ui 0.6.0, unreleased — done = the ACCENT family (accent-soft ✓ in an accent ring), never an ok recipe: a completed step is progress, not a status verdict (rule 2; the solid-ok and ok-soft mockup recipes lost); `__bar` connector is optional, caller-placed; clickability rides the element — a `<button>` only for steps the caller allows (back-nav), a `<span>` otherwise; sizes 20px default / 17px `--sm` |
| `seg` | v1 | shipped — PACKAGED | `ds/components-seg.html` | **@mythicalos/preact-ui** `Seg` · react-ui mirror (classes from **@mythicalos/ui-core** `segClass`/`segOptionClass`/`segCountText`) | ui-core 0.7.0/preact-ui 0.8.0/react-ui 0.6.0, unreleased — squared segmented tabs, NOT the theme-toggle pill (rule 10 stays scoped there); selection = accent-soft + accent-strong + bold on a surface+`--my-border` track (the house selection grammar — the raised-surface knob and the full-accent fill both lost); chips ride inside options; counts are tabular-nums, never opacity-dimmed |
| `callout` / `help` | v1 | shipped — PACKAGED | `ds/components-callout.html` | **@mythicalos/preact-ui** `Callout`, `HelpButton` · react-ui mirror (**@mythicalos/ui-core** `calloutClass`/`helpClass`; glyphs index `BANNER_ICON`) | ui-core 0.7.0/preact-ui 0.8.0/react-ui 0.6.0, unreleased — the titled tone BLOCK (a one-line notice stays a banner); `accent` = default, no modifier; `__title` OR `__kicker`, one per callout, never both (the bindings enforce it); `.my-help` stays ROUND — a PROPOSED rule-10 scoped exception (theme-toggle precedent) pending the design-book ruling, squares to `--my-r-control` if it goes the other way |
| `kpi-tile` | v1 | shipped — PACKAGED | `ds/components-kpi-tile.html` | **@mythicalos/preact-ui** `KpiTile` · react-ui mirror (geometry + class derivation in **@mythicalos/ui-core** `src/logic/kpi-tile.ts`, styled under `my-kpi-*`; label/value/sub ride the shipped `statTileClass`/`STAT_TILE_PARTS` atom rather than a fork) | ui-core 0.10.0/preact-ui 0.11.0/react-ui 0.9.0, released (repo tag v0.1.22) — four viz modes (gauge/spark/split/plain) on one fixed anatomy: label -> viz row -> footer (delta + band); tone is caller-set only when the value is out of band (quiet when healthy, the stat-tiles v2 tone axis); the mini gauge is a per-metric configurable-threshold ring (`kpiGauge`), NOT the standalone `Gauge` atom's fixed context/usage pair; split-bar segments place by cumulative-diff rounding so independently-rounded shares always tile the track exactly (`kpiSplitRects`); an absent value (the shared `STAT_TILE_EMPTY` glyph) never draws a viz artifact even if a gauge/spark/segs prop is still supplied |

Other consumers: **the Command Center** is legacy — it adopts this system wholesale
in the React rebuild, not component-by-component; don't retrofit it. The **setup
wizard** and the other family-product UIs (skuld/saga) implement from the same rows.

## Dispatching a coding agent

Prompt skeleton for "replace/implement component X in product Y":

> Implement/replace component **`<id>` v`<n>`** in `<product>` per the mythical design
> system (repo `mythical-design` —
> prefer consuming the package over copying). Read, in order: `design.md` (rules),
> `tokens.css` (canonical tokens — import the package's `tokens.css`, never fork values),
> and the component's card under `ds/` (open it in a browser to see it rendered, both themes). Reproduce **all** shown states (hover/focus/
> disabled/error/loading where applicable) using the product's established component
> patterns — this is a recreation in the target stack, not a copy-paste of the reference
> HTML. Verify against the rules-of-use block at the bottom of tokens.css (focus ring,
> status-with-icon, terminal `--my-term-*`, danger-fill scope). Run the package's
> typecheck + tests. Then update the component's row in `COMPONENTS.md`
> ("implements" column) — and nothing else in this repo.

Screen-level context (how components compose on a page): the handoff prototype in the
maintainers' internal design archive. When a dispatch reveals a spec gap, fix the spec first (template + regenerate),
then implement — never let a product fork the design.
